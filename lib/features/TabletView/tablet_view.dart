import 'package:flutter/material.dart';

class TabletAppView extends StatelessWidget {
  const TabletAppView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}